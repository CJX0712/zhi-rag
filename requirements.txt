# ZhiDa — runtime dependencies for the offline-default demo / REST service.
# No model download required (LSI embedder + Mock generator). Text splitting is
# built-in pure-Python, so langchain is NOT required at runtime.
# For the optional real dense embedder (MiniLM) / CLIP, see requirements-optional.txt.
numpy>=1.24
faiss-cpu>=1.7
scikit-learn>=1.5.0
scipy>=1.10
rank_bm25>=0.2.2
jieba>=0.42
fastapi>=0.110
uvicorn>=0.20
typer>=0.12
pypdf>=6.4.0
pyyaml>=6.0
